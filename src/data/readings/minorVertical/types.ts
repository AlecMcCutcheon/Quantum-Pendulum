/** Full vertical-axis copy for a minor pip (upright / reversed poles). */
export interface MinorVerticalEntry {
  upright?: { detail: string; guidance?: string };
  reversed?: { detail: string; guidance?: string };
}
